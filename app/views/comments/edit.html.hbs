<div class="hero-unit">
  <form id="comment-form" class="form-horizontal" action="/comments/{{params.id}}?_method=PUT" method="POST">
    <fieldset>
      <legend>Update this Comment</legend>
      {{#if params.errors}}
      <div class="control-group">
        <ul>
        {{#each params.errors }}
          <li>{{this}}</li>
        {{/each}}
        </ul>
      </div>
      {{/if}}
      
      {{{partial "form" this}}}

      <div class="form-actions">
        <input type="submit" class="btn btn-primary" value="Save" />
        <input type="submit" class="btn btn-danger" value="Delete" formaction="/comments/{{params.id}}?_method=DELETE" />
      </div>
    </fieldset>
  </form>
</div>