    <!-- Navigation bar -->
    <nav class="navbar navbar-inverse" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-nav">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">CNRV</a>
        </div>

        <div class="collapse navbar-collapse" id="bs-nav">
          <ul class="nav navbar-nav">
            <li class="dropdown">
              <a class="dropdown-toggle">Blog</a>
              <ul class="dropdown-menu">
                {{#each nav.categories}}
                <li><a href="{{categoryPath id}}">{{name}}</a></li>
                {{/each}}
              </ul>
            </li>
            {{#each nav.menus}}
            <li class="dropdown">
              <a class="dropdown-toggle">{{name}}</a>
              <ul class="dropdown-menu">
                {{#each pages}}
                <li><a href="{{pagePath id}}">{{title}}</a></li>
                {{/each}}
              </ul>
            </li>
            {{/each}}
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <li class="placeholder"></li>
            {{#if user.givenName}}
            <li class="dropdown">
              <a href="{{userPath user.id}}" class="dropdown-toggle">{{user.givenName}} {{user.familyName}}</a>
              <ul class="dropdown-menu user-logged-in">
                <li><a href="{{userPath user.id}}">My Account</a></li>
                <li><a href="/admin">Dashboard</a></li>
                <li><a href="{{logoutPath}}">Logout</a></li>
              </ul>
            </li>
            {{else}}
            {{#if user.username}}
            <li class="dropdown">
              <a href="{{userPath user.id}}" class="dropdown-toggle">{{user.username}}</a>
              <ul class="dropdown-menu user-logged-in">
                <li><a href="{{userPath user.id}}">My Account</a></li>
                <li><a href="/admin">Dashboard</a></li>
                <li><a href="{{logoutPath}}">Logout</a></li>

              </ul>
            </li>
            {{else}}
            <li class="dropdown">
              <a href="{{loginPath}}" class="dropdown-toggle">Sign In</a>
              <ul class="dropdown-menu user-logged-out">
                <li><a href="{{loginPath}}">Sign in with Email</a></li>
                <li><a href="{{facebookPath}}">Sign in with Facebook</a></li>
                <li><a href="/register">Create an Account</a></li>
              </ul>
            </li>
            {{/if}}
            {{/if}}
          </ul>
        </div>
      </div>
    </nav>

    <header class="container">
      {{#ifEqual root.url '/'}}
      <div id="carousel" class="carousel slide" data-ride="carousel">
        <!-- Wrapper for slides -->
        <div class="carousel-inner">
          <div class="item img-1 active"></div>
          <div class="item img-2"></div>
          <div class="item img-3"></div>
          <div class="item img-4"></div>
        </div>

        <!-- Controls -->
        <a class="left carousel-control" href="#carousel" data-slide="prev"><span class="glyphicon glyphicon-chevron-left"></span></a>
        <a class="right carousel-control" href="#carousel" data-slide="next"><span class="glyphicon glyphicon-chevron-right"></span></a>
      </div>
      {{else}}
      <h1>Colegiul National “Roman-Voda”</h1>
      <span>“Un colegiu pentru cine stie ce vrea, si pentru cine vrea sa stie!”</span>
      {{/ifEqual}}
    </header>
